{% extends 'base.html.twig' %}

{% block title %}Gestion de vente | Client{% endblock %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('assets/css/affiche.css') }}">{% endblock %}

{% block body %}
    <h3>Client</h3>
    <a href="{{ path('client_index') }}" class="btn btn-secondary" style="width:115px; height: 35px; color:white; text-decoration: none; font-size:12px;"><i class="fa fa-chevron-left" style="padding: 5px"></i>Retour</a>    
    <a href="{{ path('client_edit', {'id': client.id}) }}" class="btn btn-primary" style="width:110px; height: 35px; color:white; text-decoration: none; font-size:12px;  position :relative; left:5px "><i class="fa fa-edit" style="padding: 5px"></i>Modifier</a>
     {{ include('client/_delete_form.html.twig') }}
    
    <div class="affiche">
        <table class="table" style="width:55%;">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ client.id }}</td>
                </tr>
                <tr>
                    <th>Nom et prénom(s)</th>
                    <td>{{ client.cliNom }}</td>
                </tr>
                <tr>
                    <th>Contact</th>
                    <td>{{ client.cliContact }}</td>
                </tr>
                <tr>
                    <th>Adresse</th>
                    <td>{{ client.cliAdresse }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ client.cliMail }}</td>
                </tr>
                <tr>
                    <th>Observation</th>
                    <td>{{ client.cliObservation }}</td>
                </tr>
                <tr>
                    <th>Type</th>
                    <td>{{ client.cliType }}</td>
                </tr>
            </tbody>
        </table>
    </div>

{% endblock %}
