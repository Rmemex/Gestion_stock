{% extends 'base.html.twig' %}

{% block title %}Gestion de vente | Images{% endblock %}

{% block body %}
    <h3>Images</h3>
    <a href="{{ path('images_index') }}" class="btn btn-secondary" style="width:115px; height: 35px; color:white; text-decoration: none; font-size:12px;"><i class="fa fa-chevron-left" style="padding: 5px"></i>Retour</a>

    <a href="{{ path('images_edit', {'id': images.id}) }}" class="btn btn-primary" style="width:110px; height: 35px; color:white; text-decoration: none; font-size:12px;  position :relative; left:5px "><i class="fa fa-edit" style="padding: 5px"></i>Modifier</a>

    {{ include('images/_delete_form.html.twig') }}
    <table class="table" style="width:35%;">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ image.imgNom }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
