{% extends 'base.html.twig' %}

{% block title %}Gestion de vente | Détails{% endblock %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('assets/css/affiche.css') }}">{% endblock %}

{% block body %}
    <h3>Détails</h3>
    <a href="{{ path('categorie_index') }}" class="btn btn-secondary" style="width:115px; height: 35px; color:white; text-decoration: none; font-size:12px;"><i class="fa fa-chevron-left" style="padding: 5px"></i>Retour</a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}" class="btn btn-primary" style="width:110px; height: 35px; color:white; text-decoration: none; font-size:12px;  position :relative; left:5px "><i class="fa fa-edit" style="padding: 5px"></i>Modifier</a>

    {{ include('categorie/_delete_form.html.twig') }}
    <div class="affiche">
        <table class="table" style="width:20%;">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ categorie.id }}</td>
                </tr>
                <tr>
                    <th>Type</th>
                    <td>{{ categorie.catType }}</td>
                </tr>
            </tbody>
        </table>
    </div>
{% endblock %}
