{% extends 'base.html.twig' %}

{% block title %}Gestion de vente | Historique{% endblock %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('assets/css/affiche.css') }}">{% endblock %}

{% block body %}
    <h3>Historique</h3>
    
    <a href="{{ path('historique_index') }}" class="btn btn-secondary" style="width:115px; height: 35px; color:white; text-decoration: none; font-size:12px;"><i class="fa fa-chevron-left" style="padding: 5px"></i>Retour</a>

    <a href="{{ path('historique_edit', {'id': historique.id}) }}" class="btn btn-primary" style="width:110px; height: 35px; color:white; text-decoration: none; font-size:12px;  position :relative; left:5px "><i class="fa fa-edit" style="padding: 5px"></i>Modifier</a>

    {{ include('historique/_delete_form.html.twig') }}

    <div class="affiche">
        <table class="table" style="width:35%;">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ historique.id }}</td>
                </tr>
                <tr>
                    <th>Reférence</th>
                    <td>{{ historique.histoRefExterne }}</td>
                </tr>
                <tr>
                    <th>Date</th>
                    <td>{{ historique.histoDate ? historique.histoDate|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Type</th>
                    <td>{{ historique.histoType }}</td>
                </tr>
                <tr>
                    <th>Valeur</th>
                    <td>{{ historique.histoValeur }}</td>
                </tr>
                <tr>
                    <th>Observation</th>
                    <td>{{ historique.histoObservation }}</td>
                </tr>
            </tbody>
        </table>
    </div>

{% endblock %}
