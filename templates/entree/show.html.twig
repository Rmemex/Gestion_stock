{% extends 'base.html.twig' %}

{% block title %}Gestion de vente | Entree{% endblock %}

{% block stylesheets %}<link rel="stylesheet" href="{{ asset('assets/css/affiche.css') }}">{% endblock %}

{% block body %}
    <h3>Entrée</h3>
    <a href="{{ path('entree_index') }}" class="btn btn-secondary" style="width:115px; height: 35px; color:white; text-decoration: none; font-size:12px;"><i class="fa fa-chevron-left" style="padding: 5px"></i>Retour</a>

    <div class="affiche" style="top: 10px">
        <table class="table" style="width:35%;">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entree.id }}</td>
                </tr>
                <tr>
                    <th>Date</th>
                    <td>{{ entree.entrDate ? entree.entrDate|date('Y-m-d') : '' }}</td>
                </tr>
                <tr>
                    <th>Prix</th>
                    <td>{{ entree.entrPrix }}</td>
                </tr>
                <tr>
                    <th>Quantite</th>
                    <td>{{ entree.entrQuantite }}</td>
                </tr>
            </tbody>
        </table>
    </div>

{% endblock %}
