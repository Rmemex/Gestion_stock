{% extends 'base.html.twig' %}

{% block title %}Gestion de vente | Modification entrée{% endblock %}

{% block body %}
    <h3>Modification entree</h3>
    <a href="{{ path('entree_index') }}"  class="btn btn-secondary" style="color:white; text-decoration: none; font-size:12px; width:115px; height: 35px;"><i class="fa fa-chevron-left" style="padding: 5px"></i>Retour</a>

    {{ include('entree/_delete_form.html.twig') }}

    {{ form_start(form) }}
        <button class="btn btn-success" style="position:relative; top:-71px; left:121px; font-size:12px;"><i class="fa fa-edit" style="padding: 5px"></i>{{ button_label|default('Enregistrer') }}</button>
        <div style="position:relative; top:-55px; width:40%; margin:auto">
            {{ form_widget(form) }}
        </div>
    {{ form_end(form) }}
{% endblock %}
