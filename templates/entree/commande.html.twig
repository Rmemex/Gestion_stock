<head>
	<meta charset="utf-8">
    <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
    <script src="{{ asset('assets/jquery/jquery.min.js') }}"></script>
    <script src="{{ asset('assets/js/bootstrap.min.js') }}"></script>
    <script src="{{ asset('plugins/jquery.min.js') }}"></script>
	<style type="text/css">
		body{ background-color: #404C53 }

		.corps{ background-color: white;width: 45%; height: 725px; position: relative; top:-5px; margin:auto; }

		.d1, .d2 { line-height:3px; display: inline-block; font-size: 14px; }

		.d1{ position: relative; left: 25px; top:-30px; width: 200px; padding: 10px}

		.d2 { position: relative; float: right; left: 5px; top:-10px; width: 250px; padding: 10px }
	</style>
</head>
<body>
	<div class="corps" id="page">
		<div style="position:relative; top:30px ">
			<div class="d1">
				<div>
					<img src="{{ asset('assets/image/facture.png') }}" alt="logo" style=" width:120px">
				</div>
				<div>
					<p>ALAIN GSM</p>
					<p>Tel: 034 01 048 88</p>
				</div>
			</div>
			<div class="d2">
				<p><b>Bon de commande n°:</b></p>
				<p><b>Date: </b> {{ commande.entrDate ? commande.entrDate|date('Y-m-d') : '' }}</p>
				<p><b>Fornisseur: </b> {{ commande.fournisseur }}</p>
				<p><b>Contact: </b>{{ commande.fournisseur.frnContact }}</p>
				<p><b>Adresse: </b>{{ commande.fournisseur.frnAdresse }}</p>	
			</div>	

			<table class="table table-bordered" style="width:95%; margin:auto">
				<thead>
					<tr class="text-center">
						<th>Quantité</th>
						<th>Article</th>
						<th>Prix unitaire</th>
						<th>Montant</th>
					</tr>
				</thead>
				<tbody>
					{% for commande in commandes %}
					<tr class="text-center">
						<td>{{ commande.entrQuantite }}</td>
						<td>{{ commande.produit }}</td>
						<td>{{ commande.entrPrix }} Ar</td>
						<td>{{ commande.entrPrix * commande.entrQuantite }} Ar</td>
					</tr> 
					{% endfor %} 
				</tbody>
			</table>
			<div style="padding: 15px">
				{% set total = 0 %}
 					{% for commande in commandes %}
    					{% set total = total+ commande.entrPrix * commande.entrQuantite %}
 					{% endfor %}
				<p><b>Montant total: </b>{{total}} Ar</p>
			</div>
		</div>
	</div>
